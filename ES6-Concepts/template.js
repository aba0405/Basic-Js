const firstName = "Justin";
const lastName = "Rojas"
const fullName = firstName + " " + lastName + " is a very hard working programmer";
const fullName2 = `${firstName} ${lastName} is a very hard working programmer`

const multiline = "I love you"
console.log(fullName)
console.log(fullName2)